# alb-flask-mock-server

## Run server

```bash
git clone https://github.com/OpenJNY/alb-flask-mock-server.git flaskserver
cd flaskserver
docker-compose up
```

## Client

```bash
curl -v localhost:80
```